{
  "name": "random-number",
  "version": "0.2.0",
  "description": "A random number picker",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "pack": "electron-packager . random-number --platform=win32 --arch=x64 --out=out --icon=assets/app.ico --asar --overwrite --ignore=.git",
    "packager": "electron-packager ./ randomNum --win --out ./outputs --overwrite"
  },
  "author": "FlyGues",
  "license": "MIT",
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
  }
}
