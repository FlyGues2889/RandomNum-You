{
  "name": "random-number",
  "version": "0.3.7",
  "description": "A random number picker",
  "main": "main.js",
  "icon": "app/assets/appIcon.ico",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "packager": "electron-packager . randomNum --win --out ./output --icon=assets/appIcon.ico --overwrite"
  },
  "author": "FlyGues",
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
  }
}
